#include <stdio.h>
#include <stdlib.h>
#define SIZE 10

int main()
{
    int arr[SIZE] = {0,};
    5[arr] = 1;

    for(int i =0; i<SIZE  ; i++)
    {
        printf("arr[%d] = %d\n", i, arr[i]);
    }
    return 0;
}
